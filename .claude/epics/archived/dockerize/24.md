---
name: READMEへのDockerビルド・起動手順の追記
status: closed
created: 2026-08-18T15:51:12Z
updated: 2026-08-18T15:58:00Z
github: https://github.com/Sut103/discord-evenotify/issues/24
depends_on: [22, 23]
parallel: true
conflicts_with: []
---

# Task: READMEへのDockerビルド・起動手順の追記

## Description

`README.md` に、Dockerでのビルド・起動手順(`docker build`/`docker run`)および `docker-compose.yml` を使った起動手順を追記する。既存の「セットアップ」節のスタイルに合わせ、実際のDockerfile/docker-compose.ymlの内容(サービス名・環境変数名)と一致させる。

## Acceptance Criteria

- [x] README に Dockerでのビルドコマンド(`docker build -t eveping .`)と起動コマンド(`docker run -e EVEPING_DISCORD_TOKEN=... eveping`)の記載がある
- [x] README に `docker-compose.yml` を使った起動手順の記載がある
- [x] 記載されたコマンド例が実際の `Dockerfile`/`docker-compose.yml` の内容(サービス名・環境変数名)と一致している

## Test Plan

実装当時は以下の静的アサーションテスト(`dockerbuild/readme_test.go`)をred→greenで書いていたが、CLAUDE.mdのTDD方針が「アプリケーションコード(`cmd/`・`internal/`配下)の変更に限る」とスコープ限定されたため対象外(READMEはドキュメント)と判明し、シンプルさを優先してテストファイルごと削除した。以下は実施時の記録として残す。

- ~~`TestReadmeDocumentsDockerBuildAndRun`~~ — `README.md` に `docker build` と `docker run` を含む記述、および `EVEPING_DISCORD_TOKEN` への言及があることを確認
- ~~`TestReadmeDocumentsDockerCompose`~~ — `README.md` に `docker compose`(または `docker-compose`)への言及があることを確認

## Technical Details

- 変更ファイル: `README.md`
- 既存の「セットアップ」節の後に「Dockerでの起動」節を新設して追記する。

## Dependencies

- Task 1(Dockerfile)、Task 2(docker-compose.yml) — コマンド例が実物と一致している必要があるため、両方の内容が確定していること

## Effort Estimate

- Size: XS
- Hours: 1

## Definition of Done

- [x] (実装当時)Failing tests written first (red)、期待した理由で失敗することを確認した — その後TDD方針のスコープ限定によりテストファイルは削除(上記Test Plan参照)
- [x] `go test ./...` が成功する
- [x] Code reviewed(code-reviewスキルで指摘なし)
