---
name: CIへのdocker buildビルド検証ステップ追加
status: closed
created: 2026-08-18T15:51:12Z
updated: 2026-08-18T15:58:00Z
github: https://github.com/Sut103/discord-evenotify/issues/25
depends_on: [22]
parallel: true
conflicts_with: []
---

# Task: CIへのdocker buildビルド検証ステップ追加

## Description

既存の `.github/workflows/ci.yml` に `docker build .` を実行するステップ(またはジョブ)を追加し、Dockerfileのビルド失敗をPull Request時点で検知できるようにする。イメージのタグ付け・レジストリへのpush・公開は行わない(スコープ外)。

## Acceptance Criteria

- [x] `.github/workflows/ci.yml` に `docker build .` を実行するstep(または新規job)が追加されている
- [x] 既存の `test` ジョブと同じトリガー(push: main, pull_request: main)で実行される
- [x] イメージのpush・レジストリへの公開を行うstepが含まれない

## Test Plan

実装当時は以下の静的アサーションテスト(`dockerbuild/ci_workflow_test.go`)をred→greenで書いていたが、CLAUDE.mdのTDD方針が「アプリケーションコード(`cmd/`・`internal/`配下)の変更に限る」とスコープ限定されたため対象外(CI設定はビルド設定)と判明し、シンプルさを優先してテストファイルごと削除した。以下は実施時の記録として残す。

- ~~`TestCIWorkflowRunsDockerBuild`~~ — `.github/workflows/ci.yml` を読み込み、`docker build` を実行するstepが存在することを確認
- ~~`TestCIWorkflowDoesNotPushImage`~~ — `.github/workflows/ci.yml` に `docker push` や `docker/login-action` 等、レジストリへのpushを示す記述が含まれないことを確認

## Technical Details

- 変更ファイル: `.github/workflows/ci.yml`
- 既存の `test` ジョブに `docker build .` ステップを追記するか、独立した `docker-build` ジョブを追加する(GitHub Actions Ubuntuランナーは標準でDockerデーモンが使用可能なため、追加のセットアップは不要)。

## Dependencies

- Task 1(Dockerfileが存在し実際にビルド可能であること)

## Effort Estimate

- Size: XS
- Hours: 1

## Definition of Done

- [x] (実装当時)Failing tests written first (red)、期待した理由で失敗することを確認した — その後TDD方針のスコープ限定によりテストファイルは削除(上記Test Plan参照)
- [x] `go test ./...` が成功する
- [x] Code reviewed(code-reviewスキルで指摘なし)
