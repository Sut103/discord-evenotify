---
name: 開発用docker-compose.ymlの追加
status: closed
created: 2026-08-18T15:51:12Z
updated: 2026-08-18T15:58:00Z
github: https://github.com/Sut103/discord-evenotify/issues/23
depends_on: [22]
parallel: true
conflicts_with: []
---

# Task: 開発用docker-compose.ymlの追加

## Description

ローカル開発でビルド〜起動を一発で行える `docker-compose.yml` をリポジトリルートに追加する。Task 1で追加する `Dockerfile` をビルドコンテキストとして参照し、`EVEPING_DISCORD_TOKEN` は `.env` 等の環境変数経由でのみ注入する(値をファイルにハードコードしない)。

## Acceptance Criteria

- [x] リポジトリルートに `docker-compose.yml` が追加され、Task 1の `Dockerfile` をビルドコンテキストとして参照する
- [x] `EVEPING_DISCORD_TOKEN` は `environment`/`env_file` を通じて注入され、値がハードコードされていない
- [x] `docker compose config` で構文エラーなく解釈できる(静的検証)

## Test Plan

実装当時は以下の静的アサーションテスト(`dockerbuild/compose_test.go`)をred→greenで書いていたが、CLAUDE.mdのTDD方針が「アプリケーションコード(`cmd/`・`internal/`配下)の変更に限る」とスコープ限定されたため対象外と判明し、シンプルさを優先してテストファイルごと削除した。以下は実施時の記録として残す。

- ~~`TestComposeFileReferencesTokenEnvVar`~~ — `docker-compose.yml` に `EVEPING_DISCORD_TOKEN` への参照が含まれ、値がハードコードされていないことを確認
- ~~`TestComposeFileBuildsFromRepoDockerfile`~~ — `docker-compose.yml` の `build` セクションがリポジトリルート(`.`)を指していることを確認

## Technical Details

- 新規ファイル: `docker-compose.yml`
- 例:
  ```yaml
  services:
    eveping:
      build: .
      environment:
        - EVEPING_DISCORD_TOKEN=${EVEPING_DISCORD_TOKEN}
      restart: unless-stopped
  ```
- ローカルの `.env`(コミットしない、`.gitignore` で既に除外済み)に `EVEPING_DISCORD_TOKEN=...` を書く運用を想定する。

## Dependencies

- Task 1(Dockerfile) — ビルドコンテキストとして参照するため、先に確定している必要がある

## Effort Estimate

- Size: XS
- Hours: 1

## Definition of Done

- [x] (実装当時)Failing tests written first (red)、期待した理由で失敗することを確認した — その後TDD方針のスコープ限定によりテストファイルは削除(上記Test Plan参照)
- [x] `go build ./...` / `go vet ./...` / `go test ./...` が全て成功する
- [x] `docker compose config` による構文検証を実施(この実行環境にDockerデーモンは無いが、configの静的パースはデーモン不要で実行できた) — `docker compose up` による実起動確認は未実施
- [x] Code reviewed(code-reviewスキルで指摘なし)
